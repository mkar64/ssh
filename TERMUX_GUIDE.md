# 📱 دليل التشغيل والإعداد الكامل لتطبيق Termux (Android)

هذا الملف يحتوي على الدليل التفصيلي الموجه لمستخدمي تطبيق **Termux** على هواتف الأندرويد، موضحاً الأوامر، الخطوات، الحزم المطلوبة، والصلاحيات التي يحتاجها النظام للعمل بشكل مستمر ودون انقطاع.

## 🚀 أمر النسخ واللصق الشامل (All-in-One Copy & Paste Command)

> [!TIP]
> **انسخ الكود أدناه بالكامل وقم بلصقه في Termux دفعة واحدة.**
> سيقوم بتحديث الحزم، إعطاء الصلاحيات، استنساخ المستودع، التثبيت، وتشغيل الخدمة وواجهة الويب تلقائياً ومتسلسلاً:

```bash
[ -d ~/storage ] || termux-setup-storage && \
pkg update -y && \
pkg install -y git curl python openssh autossh termux-api nmap iproute2 net-tools && \
git clone https://github.com/mkar64/ssh.git && \
cd ssh && \
chmod +x setup.sh keep_ssh.sh web_ui.py && \
./setup.sh ms mycontrolbox.duckdns.org 3367 && \
nohup ./keep_ssh.sh > /dev/null 2>&1 & \
python3 web_ui.py
```

---

## 📜 البرومبت الكامل والأمر الشامل (Full Prompt & Specification)

### 1) نص البرومبت البرمجي الكامل (System Specification Prompt):

```text
قم بإعادة هيكلة وتطوير مستودع ssh ليصبح نظام إدارة واتصال متكامل وشامل لأي جهاز:

القيم الافتراضية: المنفذ الافتراضي هو 3367، الخادم الافتراضي mycontrolbox.duckdns.org، واسم المستخدم ms.

السكربت الرئيسي الشامل (setup.sh):
- يكتشف بيئة التشغيل تلقائياً (Termux على أندرويد عبر pkg، أو Linux/Ubuntu عبر apt).
- يُحدّث الحزم ويثبت جميع الأدوات والمكتبات المطلوبة دفعة واحدة: (openssh, autossh, termux-api, curl, rsync, git, python, nmap, iproute2, net-tools, fail2ban).
- يُولد مفتاح ed25519 إن لم يكن موجوداً، وينسخه تلقائياً للهدف عبر المنفذ الافتراضي 3367 أو المنفذ الممرر.
- يقبل المتغيرات الديناميكية من التيرمينال: $1 (المستخدم)، $2 (الهوست/الدومين)، $3 (المنفذ الافتراضي: 3367).
- ينشئ سكربت التشغيل الدائم keep_ssh.sh بحلقة مراقبة autossh مستمرة ويمنع السكون (wake-lock).

واجهة تحكم ويب خفيفة (web_ui.py):
- إنشاء سيرفر ويب تفاعلي وبسيط بلغة بايثون على المنفذ 8080 لتشغيل الأوامر عبر أزرار جاهزة (Quick Action Buttons).
- ميزة اكتشاف وفحص عناوين الـ IP النشطة في الشبكة المحلية وعرضها في قائمة منسدلة للاختيار منها بضغطة زر.
- أزرار سريعة لتنفيذ أوامر SSH، فحص حالة الاتصال، وإعادة تشغيل الجلسة دون الحاجة لكتابة الأوامر يدوياً.
```

### 2) الأمر الشامل للتشغيل المباشر في Termux:

```bash
pkg update -y && pkg install -y git curl && curl -sL https://raw.githubusercontent.com/mkar64/ssh/main/setup.sh | bash -s -- ms mycontrolbox.duckdns.org 3367
```

---

## 🔗 1. الروابط والأوامر التي تُكتب في التيرمينال (Termux Commands)

### أ) الأمر السريع بضغطة واحدة (One-Line Automated Command):

افتح تطبيق Termux واكتب الأمر التالي مباشرة:

```bash
pkg update -y && pkg install -y git curl && curl -sL https://raw.githubusercontent.com/mkar64/ssh/main/setup.sh | bash -s -- ms mycontrolbox.duckdns.org 3367
```

### ب) الطريقة اليدوية عبر استنساخ المستودع (Manual Git Clone):

```bash
# 1. تحديث الحزم وتثبيت git
pkg update -y && pkg install -y git

# 2. استنساخ المستودع
git clone https://github.com/mkar64/ssh.git
cd ssh

# 3. منح تصاريح التشغيل للسكربتات
chmod +x setup.sh keep_ssh.sh web_ui.py

# 4. تشغيل السكربت الشامل بالقيم الافتراضية
./setup.sh ms mycontrolbox.duckdns.org 3367
```

---

## 📋 2. الخطوات الترتيبية للتثبيت والتشغيل (Step-by-Step Guide)

1. **الخطوة الأولى: إعداد صلاحيات Termux الأولى**
   قم بتنفيذ أمر منح صلاحيات الملفات والذاكرة:
   ```bash
   termux-setup-storage
   ```
   *(ستظهر لك نافذة من الأندرويد تطلب السماح بالوصول للملفات، اضغط "سماح / Allow")*.

2. **الخطوة الثانية: تشغيل السكربت الشامل (`setup.sh`)**
   قم بتشغيل السكربت، والذي سيقوم تلقائياً بـ:
   - اكتشاف نظام Termux.
   - تثبيت كافة الحزم والمكتبات دفعة واحدة بدون تدخلك.
   - توليد مفتاح SSH الأمني من نوع `ed25519`.
   - إعداد سكربت التشغيل الدائم `keep_ssh.sh`.

3. **الخطوة الثالثة: تشغيل خدمة الاتصال المستمر بالخلفية**
   ```bash
   nohup ./keep_ssh.sh > /dev/null 2>&1 &
   ```

4. **الخطوة الرابعة: تشغيل واجهة تحكم الويب (Port 8080)**
   ```bash
   python3 web_ui.py
   ```

5. **الخطوة الخامسة: الدخول للوحة التحكم**
   افتح متصفح الهاتف (Chrome/Firefox) وادخل على العنوان:
   👉 **`http://localhost:8080`**

---

## 📦 3. التطبيقات والحزم والمصادر التي يتم تثبيتها (Packages & Apps)

### التطبيقات الخارجية الموصى بها (Recommended Android Apps):
1. **تطبيق Termux الرئيسي**: يُفضل تثبيته من متجر **F-Droid** بدلاً من Google Play للحصول على أحدث التحديثات.
2. **تطبيق Termux:API**: يُثبت من F-Droid لإعطاء السكربت قدرة التحكم بوظائف الأندرويد (مثل منع سكون الجهاز).

### الحزم والمكتبات البرمجية التي يثبتها السكربت تلقائياً عبر `pkg`:
- `openssh`: خادم وعميل SSH لتشفير وتأمين الاتصالات.
- `autossh`: أداة لإعادة الاتصال تلقائياً وبشكل مستمر في حال انقطاع شبكة الهاتف أو الواي فاي.
- `termux-api`: مكتبة الربط بين التيرمينال ونظام الأندرويد.
- `curl` & `git` & `rsync`: أدوات جلب الملفات، الاستنساخ، ونقل البيانات.
- `python`: لتشغيل واجهة التحكم الخفيفة `web_ui.py`.
- `nmap` & `iproute2` & `net-tools`: أدوات فحص واكتشاف الأجهزة والآيبيات النشطة في الشبكة المحلية LAN.

---

## 🔒 4. الصلاحيات المطلوبة والتي سيأخذها النظام (Permissions Required)

| الصلاحية (Permission) | الغرض منها (Purpose) | كيفية التفعيل (How to enable) |
| :--- | :--- | :--- |
| 📂 **صلاحية الذاكرة (Storage Access)** | حفظ مفاتيح SSH وسجلات الاتصال والملفات. | عبر الأمر `termux-setup-storage` والموافقة على التنبيه. |
| ⚡ **منع سكون المعالج (Wake-Lock)** | منع نظام الأندرويد من إيقاف المعالج (CPU) عند إغلاق الشاشة لضمان استمرار الاتصال. | يتم تفعيلها تلقائياً بالسكربت عبر `termux-wake-lock`. |
| 🔋 **إلغاء تحسين البطارية (Disable Battery Optimization)** | منع الأندرويد من قتل تطبيق Termux بالخلفية لإنقاذ البطارية. | من إعدادات الهواتف -> التطبيقات -> Termux -> البطارية -> اختيار "غير محسّن / Unrestricted". |
| 🌐 **صلاحية فتح المنافذ (Network Ports)** | استخدام المنفذ `3367` لاتصالات SSH والمنفذ `8080` لوحة التحكم بالويب. | يتم منحها تلقائياً من نظام الأندرويد للتطبيقات المحليّة. |
